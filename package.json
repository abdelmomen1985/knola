{
  "name": "knola",
  "private": true,
  "version": "0.0.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run build && lerna publish",
    "cli:link": "lerna run build --scope=@knola/cli && lerna run cli:link"
  },
  "devDependencies": {
    "@types/node": "^17.0.30",
    "lerna": "^4.0.0",
    "typescript": "^4.6.4"
  }
}
